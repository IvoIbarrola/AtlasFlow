services:
  atlas-api:
    build:
      context: .
      dockerfile: docker/Dockerfile

    container_name: atlas-api

    env_file:
      - .env

    ports:
      - "${PORT}:8000"

    restart: unless-stopped
